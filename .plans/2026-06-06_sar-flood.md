# Plan: SAR flood onset — `sar_flood`

**Date**: 2026-06-06
**Status**: COMPLETED (offline; live S1 stats deferred — no creds/network)
**Phase**: Horizon 1 — Trustworthy analyst (ROADMAP item 2 follow-up: SAR change / flood onset)

## Goal

`sar_flood(bbox, dateBefore, dateAfter, …)` — water % before vs after an event and the change
(Δ water % of the AOI). Positive Δ over a short window = flooding. All-weather.

## Approach

Compose the `sar_water` measurement across two dates (reusing the generalized `statistics()` +
`sarWaterEvalscript` + `sarSource`), run in parallel. Extract a **pure `floodResult()`** helper
(delta, rounding, low-quality flag, interpretation) so the new logic is unit-testable offline.

## Implementation steps

- [x] `tools/sar.ts`: pure `floodResult()` + `sar_flood` tool (per-date stats → delta →
      provenance ×2 + index card).
- [x] `test/sar.test.ts`: `floodResult` — flood / recede / unchanged / rounding / low-coverage.

## Testing

`pnpm test` green (65); typecheck (src+test) + build green; MCP lists `sar_flood` (12 tools).
Live S1 stats + threshold tuning deferred (no creds/network).

## Definition of done

- [x] builds + typechecks (src+test); tests green; MCP lists `sar_flood`
- [x] CONTINUITY.md + PROGRESS.md + ROADMAP.md (+ README) updated

## Notes / log

- 2026-06-06: small composition of tested pieces; the only new logic (delta/interpretation) is
  isolated in a pure helper and unit-tested. Reused the `index` card (Δ in the title).
